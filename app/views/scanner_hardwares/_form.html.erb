<%= form_for(@scanner_hardware) do |f| %>
  <% if @scanner_hardware.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@scanner_hardware.errors.count, "error") %> prohibited this scanner_hardware from being saved:</h2>

      <ul>
      <% @scanner_hardware.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :hardware_group %><br />
    <%= f.text_field :hardware_group %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>

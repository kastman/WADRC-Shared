require 'spec_helper'

describe "SeriesSets" do
  describe "GET /series_sets" do
    it "works! (now write some real specs)" do
      visit series_sets_path
      response.status.should be(200)
    end
  end
end

require 'spec_helper'

describe "SeriesScenarios" do
  describe "GET /series_scenarios" do
    it "works! (now write some real specs)" do
      visit series_scenarios_path
      # response.status.should be(200)
    end
  end
end

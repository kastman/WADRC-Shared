require 'spec_helper'

describe "MriScans" do
  describe "GET /mri_scans" do
    it "works! (now write some real specs)" do
      visit mri_scans_path
      # response.status.should be(200)
    end
  end
end

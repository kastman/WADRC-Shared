require 'spec_helper'

describe "FunctionalSets" do
  describe "GET /functional_sets" do
    it "works! (now write some real specs)" do
      visit functional_sets_path
      # response.status.should be(200)
    end
  end
end
